{
  "name": "package-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "nodemon index.js",
    "debug": "nodemon --inspect=0.0.0.0:5000 index.js",
    "kill": "kill $(lsof -t -i:5000)"
  },
  "license": "MIT",
  "devDependencies": {
    "express": "^4.18.1",
    "socket.io-mock": "^1.3.2",
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "socket.io": "^4.5.2",
    "dotenv": "^16.0.2"
  }
}
